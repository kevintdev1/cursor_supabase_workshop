---
import { getLangFromUrl, useTranslations } from '../i18n/utils';
import { Rocket } from '@lucide/astro';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section class="py-32 relative overflow-hidden">
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[500px] bg-brand-supabase/5 blur-[120px] rounded-full pointer-events-none no-print"></div>

  <div class="relative mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 text-center">
    <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-brand-supabase/10 text-brand-supabase mb-8 border border-brand-supabase/20">
      <Rocket class="w-8 h-8" />
    </div>
    
    <h2 class="text-4xl md:text-5xl font-bold tracking-tight text-white mb-6">
      {t('cta.title')}
    </h2>
    
    <p class="text-xl text-zinc-400 mb-12 max-w-2xl mx-auto leading-relaxed">
      {t('cta.desc')}
    </p>

    <button 
      id="cta-confetti-btn"
      class="group relative inline-flex items-center justify-center gap-3 px-8 py-4 text-lg font-bold text-black bg-brand-supabase rounded-full overflow-hidden transition-transform cursor-pointer hover:scale-105 hover:shadow-[0_0_40px_-10px_#3ecf8e]"
    >
      <div class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-in-out"></div>
      <span class="relative">{t('cta.btn')}</span>
    </button>
  </div>
</section>

<script>
  import confetti from 'canvas-confetti';

  const button = document.getElementById('cta-confetti-btn');
  const repoUrl = 'https://github.com/martinruizn/supaday-usfq.git';
  
  if (button) {
    button.addEventListener('click', () => {
      const hasSeenConfetti = localStorage.getItem('hasSeenConfetti');
      
      if (hasSeenConfetti) {
        window.open(repoUrl, '_blank');
        return;
      }

      localStorage.setItem('hasSeenConfetti', 'true');
      
      const duration = 3 * 1000;
      const animationEnd = Date.now() + duration;
      const defaults = { startVelocity: 30, spread: 360, ticks: 60, zIndex: 0 };

      function randomInRange(min: number, max: number) {
        return Math.random() * (max - min) + min;
      }

      const interval: any = setInterval(function() {
        const timeLeft = animationEnd - Date.now();

        if (timeLeft <= 0) {
          clearInterval(interval);
          window.open(repoUrl, '_blank');
          return;
        }

        const particleCount = 50 * (timeLeft / duration);
        confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.1, 0.3), y: Math.random() - 0.2 } }));
        confetti(Object.assign({}, defaults, { particleCount, origin: { x: randomInRange(0.7, 0.9), y: Math.random() - 0.2 } }));
      }, 250);
    });
  }
</script>
